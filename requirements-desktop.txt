# Optional — Windows desktop UI automation (local FlowTest / MCP only).
# Do not add these to Streamlit Cloud requirements.txt.
pywinauto>=0.6.8,<0.7
Pillow>=10.0.0,<12
comtypes>=1.4.0,<2
pynput>=1.7.6,<2
