# Pin Python via runtime.txt (python-3.11). Streamlit Cloud's default 3.14
# cannot build greenlet (Playwright dependency) or pandas wheels reliably.
streamlit>=1.32.0,<1.46
pandas>=2.0.0,<2.3
numpy>=1.24.0,<2.1
plotly>=5.18.0,<6
playwright==1.49.0
httpx>=0.27.0,<0.29
