{
  "id": "tst_685fa3507a",
  "name": "Coverage: TUI navigation & CTAs",
  "description": "Optional clicks on common Dutch nav/CTA labels; returns home between attempts.",
  "tags": [
    "ui",
    "nav",
    "tui",
    "coverage"
  ],
  "suite": "UI Coverage",
  "version": 3,
  "steps": [
    {
      "id": "stp_8bd16c8749",
      "type": "ui.goto",
      "name": "Open TUI homepage",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_c6cf696aea",
      "type": "ui.wait",
      "name": "Wait for cookie dialog",
      "config": {
        "ms": 2800
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_6549c29df4",
      "type": "util.custom_js",
      "name": "Click Accepteer cookies",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": "TUI modal 'We respecteren jouw privacy' \u2014 clicks Accepteer cookies, never Weigeren"
    },
    {
      "id": "stp_f603f43994",
      "type": "ui.wait",
      "name": "Settle after consent",
      "config": {
        "ms": 1500
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_e6de7d6ef7",
      "type": "util.custom_js",
      "name": "Optional click \"Vakanties\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Vakanties'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_d5bc6d713b",
      "type": "ui.wait",
      "name": "After Vakanties",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_092ff045c2",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_0291e42773",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_50f009e616",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_0771beb4a2",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_b0b29767fd",
      "type": "util.custom_js",
      "name": "Optional click \"Vliegtickets\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Vliegtickets'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_9fa203dc4d",
      "type": "ui.wait",
      "name": "After Vliegtickets",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_484abf4ab9",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_a468c5ae1d",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_2ebe7c4f30",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_3314919637",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_cda2cde9d5",
      "type": "util.custom_js",
      "name": "Optional click \"Cruises\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Cruises'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_b431ea9b5c",
      "type": "ui.wait",
      "name": "After Cruises",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_99f48a8cdb",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_b66b11f88d",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_b109e63948",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_6ab668e3f1",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_cd4ff13f73",
      "type": "util.custom_js",
      "name": "Optional click \"Zoeken\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Zoeken'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_5a133baef5",
      "type": "ui.wait",
      "name": "After Zoeken",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_4f7239aa1f",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_f6dd169709",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_7349cdee39",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_8b661f4008",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_fddc9461dc",
      "type": "util.custom_js",
      "name": "Optional click \"Inloggen\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Inloggen'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_a7e5203ee8",
      "type": "ui.wait",
      "name": "After Inloggen",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_fb7a569219",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_597e76ae77",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_41f2f0f1d4",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_fad33404f0",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_42b257079a",
      "type": "util.custom_js",
      "name": "Optional click \"Service & Contact\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Service & Contact'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_3e780e8580",
      "type": "ui.wait",
      "name": "After Service & Contact",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_0393f3a5a2",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_982dc81d2d",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_4e15f99a17",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_adddc5d1ea",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_275f8bd213",
      "type": "util.custom_js",
      "name": "Optional click \"Bestemmingen\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Bestemmingen'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_11a99b9902",
      "type": "ui.wait",
      "name": "After Bestemmingen",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_c486ac212c",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_60ba913762",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_003b8f8eb9",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_1b2b905277",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_051b10f258",
      "type": "util.custom_js",
      "name": "Optional click \"Last minute\"",
      "config": {
        "script": "\n(() => {\n  const want = 'Last minute'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
      },
      "enabled": true,
      "notes": "Non-fatal if control missing (A-B UI)"
    },
    {
      "id": "stp_b2ae5bb303",
      "type": "ui.wait",
      "name": "After Last minute",
      "config": {
        "ms": 700
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_b1588b30db",
      "type": "ui.goto",
      "name": "Return home",
      "config": {
        "url": "{{BASE_URL}}",
        "timeout_ms": 60000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_96efb34fb1",
      "type": "ui.wait",
      "name": "Home settle",
      "config": {
        "ms": 900
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_45589939f7",
      "type": "util.custom_js",
      "name": "Re-dismiss cookies if shown",
      "config": {
        "script": "\n(() => {\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      const t = (el.innerText || el.value || el.getAttribute('aria-label') || '')\n        .replace(/\\s+/g, ' ')\n        .trim()\n        .toLowerCase();\n      if (!t) continue;\n      // Prefer exact-ish accept, never click Weigeren/Refuse\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit) return hit;\n  }\n  return clickMatch(document.body) || 'no-banner';\n})()\n"
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_6b0c033dc7",
      "type": "ui.wait",
      "name": "After re-dismiss",
      "config": {
        "ms": 400
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_f8b79b6103",
      "type": "assert.element_exists",
      "name": "Home still loads after nav trail",
      "config": {
        "selector": "body",
        "timeout_ms": 10000
      },
      "enabled": true,
      "notes": ""
    },
    {
      "id": "stp_3a4a5781ec",
      "type": "ui.screenshot",
      "name": "After nav coverage",
      "config": {
        "name": "tui_nav"
      },
      "enabled": true,
      "notes": ""
    }
  ]
}