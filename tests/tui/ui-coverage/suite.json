{
  "format": "flowtest-suite/v1",
  "exported_at": "2026-09-07T09:39:16Z",
  "project_id": "prj_5aa1d5ded1",
  "project_name": "TUI",
  "suite": "UI Coverage",
  "environment_hint": "TUI Prod",
  "tests": [
    {
      "id": "tst_685fa3507a",
      "name": "Coverage: TUI navigation & CTAs",
      "description": "Required Playwright clicks on Vakanties/Vliegtickets/Cruises/Zoeken/Inloggen \u2014 FAIL if blocked.",
      "tags": [
        "ui",
        "nav",
        "tui",
        "coverage"
      ],
      "suite": "UI Coverage",
      "version": 15,
      "steps": [
        {
          "id": "stp_a2d3253d8e",
          "type": "ui.goto",
          "name": "Open TUI homepage",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_3c8c229b30",
          "type": "ui.wait",
          "name": "Wait for cookie dialog",
          "config": {
            "ms": 2800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_0035affb8e",
          "type": "util.custom_js",
          "name": "Click Accepteer cookies",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const modalVisible = () => {\n    const dialogs = Array.from(\n      document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n    );\n    for (const d of dialogs) {\n      if (!isVisible(d)) continue;\n      const t = (d.innerText || '').toLowerCase();\n      if (t.includes('we respecteren jouw privacy') || t.includes('accepteer cookies')) return true;\n    }\n    const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n    return nodes.some((el) => {\n      if (!isVisible(el)) return false;\n      return labelOf(el).includes('accepteer cookies');\n    });\n  };\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  let hit = null;\n  for (const d of dialogs) {\n    hit = clickMatch(d);\n    if (hit) break;\n  }\n  if (!hit) hit = clickMatch(document.body);\n  if (hit) return hit;\n  if (modalVisible()) {\n    throw new Error('Cookie privacy modal still visible \u2014 failed to click Accepteer cookies');\n  }\n  return 'no-banner';\n})()\n"
          },
          "enabled": true,
          "notes": "Throws if privacy modal remains after click attempt"
        },
        {
          "id": "stp_9437506fab",
          "type": "ui.wait",
          "name": "Settle after consent",
          "config": {
            "ms": 1200
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_0523fc5956",
          "type": "util.custom_js",
          "name": "Assert cookie modal gone",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": "Hard fail if We respecteren jouw privacy / Accepteer cookies still visible"
        },
        {
          "id": "stp_7351a42417",
          "type": "ui.click_by_text",
          "name": "Click \"Vakanties\"",
          "config": {
            "text": "Vakanties",
            "exact": false,
            "role": "link",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "Required \u2014 FAIL if blocked by cookie modal or missing"
        },
        {
          "id": "stp_6abacfac74",
          "type": "ui.wait",
          "name": "After Vakanties",
          "config": {
            "ms": 800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_5ae2c61886",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_e4833b5323",
          "type": "ui.wait",
          "name": "Home settle",
          "config": {
            "ms": 900
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_3174afe830",
          "type": "util.custom_js",
          "name": "Re-dismiss cookies if shown",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_09673e2dcb",
          "type": "ui.wait",
          "name": "After re-dismiss",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_da732f7bcb",
          "type": "util.custom_js",
          "name": "Assert modal not blocking",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_7ef4d40a3e",
          "type": "ui.click_by_text",
          "name": "Click \"Vliegtickets\"",
          "config": {
            "text": "Vliegtickets",
            "exact": false,
            "role": "link",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "Required \u2014 FAIL if blocked by cookie modal or missing"
        },
        {
          "id": "stp_e4025753ae",
          "type": "ui.wait",
          "name": "After Vliegtickets",
          "config": {
            "ms": 800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_99a0d08882",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_15ba537972",
          "type": "ui.wait",
          "name": "Home settle",
          "config": {
            "ms": 900
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_5d8e736113",
          "type": "util.custom_js",
          "name": "Re-dismiss cookies if shown",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_9326a86788",
          "type": "ui.wait",
          "name": "After re-dismiss",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_341b797e1d",
          "type": "util.custom_js",
          "name": "Assert modal not blocking",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_185a7ddcf3",
          "type": "ui.click_by_text",
          "name": "Click \"Cruises\"",
          "config": {
            "text": "Cruises",
            "exact": false,
            "role": "link",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "Required \u2014 FAIL if blocked by cookie modal or missing"
        },
        {
          "id": "stp_d4b31f483e",
          "type": "ui.wait",
          "name": "After Cruises",
          "config": {
            "ms": 800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_7d1da18108",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_dc075fcb57",
          "type": "ui.wait",
          "name": "Home settle",
          "config": {
            "ms": 900
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_6b7a6d8ed9",
          "type": "util.custom_js",
          "name": "Re-dismiss cookies if shown",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_91bb461cb6",
          "type": "ui.wait",
          "name": "After re-dismiss",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_ead32020be",
          "type": "util.custom_js",
          "name": "Assert modal not blocking",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_87c68ff10f",
          "type": "ui.click_by_text",
          "name": "Click \"Zoeken\"",
          "config": {
            "text": "Zoeken",
            "exact": false,
            "role": "",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "Required \u2014 FAIL if blocked by cookie modal or missing"
        },
        {
          "id": "stp_2fceaf85b2",
          "type": "ui.wait",
          "name": "After Zoeken",
          "config": {
            "ms": 800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_70b29669a4",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_a1f65b6639",
          "type": "ui.wait",
          "name": "Home settle",
          "config": {
            "ms": 900
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_7c87d3c698",
          "type": "util.custom_js",
          "name": "Re-dismiss cookies if shown",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_10aabdf85b",
          "type": "ui.wait",
          "name": "After re-dismiss",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_2762fd69b3",
          "type": "util.custom_js",
          "name": "Assert modal not blocking",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_236a774a2b",
          "type": "ui.click_by_text",
          "name": "Click \"Inloggen\"",
          "config": {
            "text": "Inloggen",
            "exact": false,
            "role": "link",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "Required \u2014 FAIL if blocked by cookie modal or missing"
        },
        {
          "id": "stp_15b3a945e6",
          "type": "ui.wait",
          "name": "After Inloggen",
          "config": {
            "ms": 800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_e76930f53c",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_6a1ddd2a89",
          "type": "ui.wait",
          "name": "Home settle",
          "config": {
            "ms": 900
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_e032e2b2cb",
          "type": "util.custom_js",
          "name": "Re-dismiss cookies if shown",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_4435dcbb5d",
          "type": "ui.wait",
          "name": "After re-dismiss",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_f2840e1253",
          "type": "util.custom_js",
          "name": "Assert modal not blocking",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_64ab0d8db3",
          "type": "util.custom_js",
          "name": "Optional click \"Service & Contact\"",
          "config": {
            "script": "\n(() => {\n  const want = 'Service & Contact'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
          },
          "enabled": true,
          "notes": "Optional \u2014 does not fail if missing"
        },
        {
          "id": "stp_fc8710f187",
          "type": "ui.wait",
          "name": "After optional Service & Contact",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_5ed976e3a6",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_9b05912b31",
          "type": "util.custom_js",
          "name": "Soft cookie dismiss",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_a147fff5f5",
          "type": "util.custom_js",
          "name": "Optional click \"Bestemmingen\"",
          "config": {
            "script": "\n(() => {\n  const want = 'Bestemmingen'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
          },
          "enabled": true,
          "notes": "Optional \u2014 does not fail if missing"
        },
        {
          "id": "stp_f984815d6d",
          "type": "ui.wait",
          "name": "After optional Bestemmingen",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_3089ad6cef",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_59e7a2d9f9",
          "type": "util.custom_js",
          "name": "Soft cookie dismiss",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_2df0da21e0",
          "type": "util.custom_js",
          "name": "Optional click \"Last minute\"",
          "config": {
            "script": "\n(() => {\n  const want = 'Last minute'.toLowerCase();\n  const nodes = Array.from(document.querySelectorAll('a, button, [role=\"button\"], [role=\"link\"]'));\n  for (const el of nodes) {\n    const t = (el.innerText || el.getAttribute('aria-label') || '').replace(/\\s+/g, ' ').trim();\n    if (t && t.toLowerCase().includes(want)) {\n      el.click();\n      return 'clicked:' + t.slice(0, 60);\n    }\n  }\n  return 'missing';\n})()\n"
          },
          "enabled": true,
          "notes": "Optional \u2014 does not fail if missing"
        },
        {
          "id": "stp_5ca3296099",
          "type": "ui.wait",
          "name": "After optional Last minute",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_b12a968482",
          "type": "ui.goto",
          "name": "Return home",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_6d2148b008",
          "type": "util.custom_js",
          "name": "Soft cookie dismiss",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies', 'alles accepteren', 'accept cookies', 'accept all',\n    'accepteer', 'accepteren', 'akkoord', 'agree', 'toestaan',\n  ];\n  const clickMatch = (root) => {\n    if (!root) return 'no-banner';\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return 'no-banner';\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    const hit = clickMatch(d);\n    if (hit.startsWith('clicked:')) return hit;\n  }\n  return clickMatch(document.body);\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_bff5067b77",
          "type": "assert.element_exists",
          "name": "Home still loads after nav trail",
          "config": {
            "selector": "body",
            "timeout_ms": 10000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_727b46532c",
          "type": "ui.screenshot",
          "name": "After nav coverage",
          "config": {
            "name": "tui_nav"
          },
          "enabled": true,
          "notes": ""
        }
      ]
    },
    {
      "id": "tst_7dd73062c7",
      "name": "Coverage: TUI search / form fields",
      "description": "After cookies, must find destination/date fields and click Zoeken \u2014 FAIL otherwise.",
      "tags": [
        "ui",
        "search",
        "form",
        "tui",
        "coverage"
      ],
      "suite": "UI Coverage",
      "version": 15,
      "steps": [
        {
          "id": "stp_bd0f5f00e5",
          "type": "ui.goto",
          "name": "Open TUI homepage",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_26bdd459b8",
          "type": "ui.wait",
          "name": "Wait for cookie dialog",
          "config": {
            "ms": 2800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_d68d0fc99f",
          "type": "util.custom_js",
          "name": "Click Accepteer cookies",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const modalVisible = () => {\n    const dialogs = Array.from(\n      document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n    );\n    for (const d of dialogs) {\n      if (!isVisible(d)) continue;\n      const t = (d.innerText || '').toLowerCase();\n      if (t.includes('we respecteren jouw privacy') || t.includes('accepteer cookies')) return true;\n    }\n    const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n    return nodes.some((el) => {\n      if (!isVisible(el)) return false;\n      return labelOf(el).includes('accepteer cookies');\n    });\n  };\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  let hit = null;\n  for (const d of dialogs) {\n    hit = clickMatch(d);\n    if (hit) break;\n  }\n  if (!hit) hit = clickMatch(document.body);\n  if (hit) return hit;\n  if (modalVisible()) {\n    throw new Error('Cookie privacy modal still visible \u2014 failed to click Accepteer cookies');\n  }\n  return 'no-banner';\n})()\n"
          },
          "enabled": true,
          "notes": "Throws if privacy modal remains after click attempt"
        },
        {
          "id": "stp_17a09da10f",
          "type": "ui.wait",
          "name": "Settle after consent",
          "config": {
            "ms": 1200
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_f077297701",
          "type": "util.custom_js",
          "name": "Assert cookie modal gone",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": "Hard fail if We respecteren jouw privacy / Accepteer cookies still visible"
        },
        {
          "id": "stp_994aa4e68d",
          "type": "util.custom_js",
          "name": "Fill destination / date fields",
          "config": {
            "script": "\n(() => {\n  const tryFill = (hints, value) => {\n    const inputs = Array.from(document.querySelectorAll('input, textarea, [contenteditable=\"true\"]'));\n    for (const el of inputs) {\n      const meta = [\n        el.getAttribute('name') || '',\n        el.getAttribute('placeholder') || '',\n        el.getAttribute('aria-label') || '',\n        el.id || '',\n      ].join(' ').toLowerCase();\n      if (!hints.some((h) => meta.includes(h))) continue;\n      if (el.isContentEditable) { el.textContent = value; }\n      else {\n        el.focus();\n        el.value = value;\n        el.dispatchEvent(new Event('input', { bubbles: true }));\n        el.dispatchEvent(new Event('change', { bubbles: true }));\n      }\n      return 'filled:' + meta.slice(0, 40);\n    }\n    return 'no-field';\n  };\n  const a = tryFill(['bestemming', 'destination', 'waarheen', 'zoek', 'search', 'stad', 'hotel'], 'Spanje');\n  const b = tryFill(['vertrek', 'depart', 'date', 'datum'], '2026-10-01');\n  if (a === 'no-field' && b === 'no-field') {\n    throw new Error('No searchable destination/date field found after cookie accept');\n  }\n  return a + '|' + b;\n})()\n"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_d333e46003",
          "type": "ui.wait",
          "name": "After fill",
          "config": {
            "ms": 500
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_c9398e991c",
          "type": "ui.click_by_text",
          "name": "Click Zoeken",
          "config": {
            "text": "Zoeken",
            "exact": false,
            "role": "button",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "Required \u2014 FAIL if search CTA blocked/missing"
        },
        {
          "id": "stp_d9dc0b2404",
          "type": "ui.wait",
          "name": "Wait for search reaction",
          "config": {
            "ms": 2000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_c9722df856",
          "type": "assert.element_exists",
          "name": "Page responsive after search interaction",
          "config": {
            "selector": "body",
            "timeout_ms": 10000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_6941f3f53a",
          "type": "ui.screenshot",
          "name": "After search interaction",
          "config": {
            "name": "tui_search"
          },
          "enabled": true,
          "notes": ""
        }
      ]
    },
    {
      "id": "tst_e933d388f2",
      "name": "Smoke: Campaign landing (UTM)",
      "description": "Opens LANDING_URL, must clear cookies, assert TUI, and click Vakanties.",
      "tags": [
        "smoke",
        "ui",
        "tui",
        "utm",
        "coverage"
      ],
      "suite": "UI Coverage",
      "version": 14,
      "steps": [
        {
          "id": "stp_35cb25af59",
          "type": "ui.goto",
          "name": "Open campaign landing URL",
          "config": {
            "url": "{{LANDING_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": "Uses LANDING_URL from environment (UTM campaign link)"
        },
        {
          "id": "stp_9b90358329",
          "type": "ui.wait",
          "name": "Wait for cookie dialog",
          "config": {
            "ms": 2800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_71381917bf",
          "type": "util.custom_js",
          "name": "Click Accepteer cookies",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const modalVisible = () => {\n    const dialogs = Array.from(\n      document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n    );\n    for (const d of dialogs) {\n      if (!isVisible(d)) continue;\n      const t = (d.innerText || '').toLowerCase();\n      if (t.includes('we respecteren jouw privacy') || t.includes('accepteer cookies')) return true;\n    }\n    const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n    return nodes.some((el) => {\n      if (!isVisible(el)) return false;\n      return labelOf(el).includes('accepteer cookies');\n    });\n  };\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  let hit = null;\n  for (const d of dialogs) {\n    hit = clickMatch(d);\n    if (hit) break;\n  }\n  if (!hit) hit = clickMatch(document.body);\n  if (hit) return hit;\n  if (modalVisible()) {\n    throw new Error('Cookie privacy modal still visible \u2014 failed to click Accepteer cookies');\n  }\n  return 'no-banner';\n})()\n"
          },
          "enabled": true,
          "notes": "Throws if privacy modal remains after click attempt"
        },
        {
          "id": "stp_8cf40642f9",
          "type": "ui.wait",
          "name": "Settle after consent",
          "config": {
            "ms": 1200
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_542dc5515a",
          "type": "util.custom_js",
          "name": "Assert cookie modal gone",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": "Hard fail if We respecteren jouw privacy / Accepteer cookies still visible"
        },
        {
          "id": "stp_234e4b9d2f",
          "type": "assert.element_exists",
          "name": "Landing document loaded",
          "config": {
            "selector": "body",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_76bff91e1e",
          "type": "assert.text_contains",
          "name": "Page shows TUI",
          "config": {
            "selector": "body",
            "text": "TUI",
            "timeout_ms": 15000,
            "ignore_case": true
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_cd0fb36e70",
          "type": "ui.click_by_text",
          "name": "Landing nav Vakanties clickable",
          "config": {
            "text": "Vakanties",
            "exact": false,
            "role": "link",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "FAIL if cookie modal still blocks nav"
        },
        {
          "id": "stp_06c58bd7fa",
          "type": "ui.screenshot",
          "name": "Landing screenshot",
          "config": {
            "name": "tui_landing_utm"
          },
          "enabled": true,
          "notes": ""
        }
      ]
    },
    {
      "id": "tst_6341515090",
      "name": "Smoke: TUI homepage load & brand",
      "description": "Opens tui.nl, MUST accept cookies, asserts branding, verifies Vakanties nav is clickable.",
      "tags": [
        "smoke",
        "ui",
        "tui",
        "coverage"
      ],
      "suite": "UI Coverage",
      "version": 15,
      "steps": [
        {
          "id": "stp_328acd89e8",
          "type": "ui.goto",
          "name": "Open TUI homepage",
          "config": {
            "url": "{{BASE_URL}}",
            "timeout_ms": 60000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_f1d2e5dcb6",
          "type": "ui.wait",
          "name": "Wait for cookie dialog",
          "config": {
            "ms": 2800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_84d375b742",
          "type": "util.custom_js",
          "name": "Click Accepteer cookies",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  const labels = [\n    'accepteer cookies',\n    'alles accepteren',\n    'accept cookies',\n    'accept all',\n    'accepteer',\n    'accepteren',\n    'akkoord',\n    'agree',\n    'toestaan',\n  ];\n  const modalVisible = () => {\n    const dialogs = Array.from(\n      document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n    );\n    for (const d of dialogs) {\n      if (!isVisible(d)) continue;\n      const t = (d.innerText || '').toLowerCase();\n      if (t.includes('we respecteren jouw privacy') || t.includes('accepteer cookies')) return true;\n    }\n    const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n    return nodes.some((el) => {\n      if (!isVisible(el)) return false;\n      return labelOf(el).includes('accepteer cookies');\n    });\n  };\n  const clickMatch = (root) => {\n    if (!root) return null;\n    const nodes = Array.from(root.querySelectorAll('button, [role=\"button\"], a, input[type=\"button\"], input[type=\"submit\"]'));\n    for (const el of nodes) {\n      if (!isVisible(el)) continue;\n      const t = labelOf(el);\n      if (!t) continue;\n      if (t.includes('weiger') || t.includes('refus') || t.includes('decline') || t.includes('reject')) continue;\n      if (labels.some((l) => t === l || t.includes(l))) {\n        el.click();\n        return 'clicked:' + t.slice(0, 48);\n      }\n    }\n    return null;\n  };\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  let hit = null;\n  for (const d of dialogs) {\n    hit = clickMatch(d);\n    if (hit) break;\n  }\n  if (!hit) hit = clickMatch(document.body);\n  if (hit) return hit;\n  if (modalVisible()) {\n    throw new Error('Cookie privacy modal still visible \u2014 failed to click Accepteer cookies');\n  }\n  return 'no-banner';\n})()\n"
          },
          "enabled": true,
          "notes": "Throws if privacy modal remains after click attempt"
        },
        {
          "id": "stp_cc18d44ae3",
          "type": "ui.wait",
          "name": "Settle after consent",
          "config": {
            "ms": 1200
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_6399a685de",
          "type": "util.custom_js",
          "name": "Assert cookie modal gone",
          "config": {
            "script": "\n(() => {\n\n  const isVisible = (el) => {\n    if (!el || !(el instanceof Element)) return false;\n    if (el.closest('[hidden], [aria-hidden=\"true\"]')) return false;\n    if (typeof el.checkVisibility === 'function') {\n      try {\n        return el.checkVisibility({ checkOpacity: true, checkVisibilityCSS: true });\n      } catch (e) { /* fall through */ }\n    }\n    const style = window.getComputedStyle(el);\n    if (!style || style.display === 'none' || style.visibility === 'hidden' || Number(style.opacity) === 0) {\n      return false;\n    }\n    const r = el.getBoundingClientRect();\n    return r.width > 2 && r.height > 2;\n  };\n  const labelOf = (el) =>\n    (el.innerText || el.value || el.getAttribute('aria-label') || el.textContent || '')\n      .replace(/\\s+/g, ' ')\n      .trim()\n      .toLowerCase();\n\n  // Only fail when the privacy UI is actually on-screen.\n  // Consent SDKs often leave \"Accepteer cookies\" nodes in the DOM after dismiss.\n  const dialogs = Array.from(\n    document.querySelectorAll('[role=\"dialog\"], [aria-modal=\"true\"], [class*=\"cookie\" i], [class*=\"consent\" i], [id*=\"cookie\" i], [id*=\"consent\" i]')\n  );\n  for (const d of dialogs) {\n    if (!isVisible(d)) continue;\n    const t = (d.innerText || '').toLowerCase();\n    if (t.includes('we respecteren jouw privacy')) {\n      throw new Error('Privacy cookie modal still blocking the page');\n    }\n  }\n  const nodes = Array.from(document.querySelectorAll('button, [role=\"button\"]'));\n  const still = nodes.some((el) => {\n    if (!isVisible(el)) return false;\n    return labelOf(el).includes('accepteer cookies');\n  });\n  if (still) throw new Error('Accepteer cookies button still visible');\n  return 'modal-gone';\n})()\n",
            "expect_contains": "modal-gone"
          },
          "enabled": true,
          "notes": "Hard fail if We respecteren jouw privacy / Accepteer cookies still visible"
        },
        {
          "id": "stp_7a75dbac8d",
          "type": "assert.title_contains",
          "name": "Title mentions TUI",
          "config": {
            "text": "TUI",
            "timeout_ms": 20000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_6bc89be764",
          "type": "assert.text_contains",
          "name": "Body mentions TUI / vakantie",
          "config": {
            "selector": "body",
            "text": "TUI",
            "timeout_ms": 20000,
            "ignore_case": true
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_e92bc02358",
          "type": "ui.click_by_text",
          "name": "Header Vakanties is clickable",
          "config": {
            "text": "Vakanties",
            "exact": false,
            "role": "link",
            "within": "",
            "timeout_ms": 15000
          },
          "enabled": true,
          "notes": "Fails if cookie overlay still blocks navigation"
        },
        {
          "id": "stp_2581f9b7e1",
          "type": "ui.wait",
          "name": "After Vakanties",
          "config": {
            "ms": 800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_8016707c21",
          "type": "util.custom_js",
          "name": "Scroll below fold",
          "config": {
            "script": "window.scrollBy(0, 1000);"
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_972a7e0c52",
          "type": "ui.wait",
          "name": "Wait after scroll",
          "config": {
            "ms": 800
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_919e1cab49",
          "type": "assert.element_exists",
          "name": "Document still responsive",
          "config": {
            "selector": "body",
            "timeout_ms": 10000
          },
          "enabled": true,
          "notes": ""
        },
        {
          "id": "stp_f971703402",
          "type": "ui.screenshot",
          "name": "Homepage screenshot",
          "config": {
            "name": "tui_home"
          },
          "enabled": true,
          "notes": ""
        }
      ]
    }
  ]
}